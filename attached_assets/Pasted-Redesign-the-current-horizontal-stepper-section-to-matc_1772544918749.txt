Redesign the current horizontal stepper section to match the visual format and layout style of the reference architecture diagram.

🔁 Layout Transformation

Convert the current horizontal 5-step timeline (User Profile → Semantic Match → Document Parse → Recommendation → WhatsApp Delivery) into a central AI hub architecture layout with the following structure:

🧩 New Structure Requirements

Center Element

Place a circular AI core in the center.

Use a clean, minimal OpenAI-style icon in black.

Add soft shadow and subtle depth.

Background should be white with light gray border.

Left Side (Input Sources)
Create 5 circular floating input nodes vertically aligned on the left:

Google Drive

Google Docs

WhatsApp

Messenger

Notion

Each:

Must be inside soft white circular containers

Use subtle drop shadow

Connected to the central AI core via thin curved lines

Use light gray connection lines

Add subtle gradient highlight animation on active connection (optional)

Right Side (User Output)

Add a single circular user icon node on the right

Connected from the central AI core

Keep it minimal and clean

🎨 Visual Style Guidelines

Background: Very light gray (#F5F5F5 or similar)

Icons: Clean, minimal, brand-accurate logos

Container Style:

Rounded circular cards

Soft shadows (low blur, low opacity)

Minimal glass / neumorphic feel

Lines:

Thin (1–2px)

Soft gray

Smooth bezier curves

No heavy borders

No bold orange timeline bars

Remove step progress bar completely

🧠 Conceptual Shift

The section should visually communicate:

Instead of:

A step-by-step progress timeline

It should now communicate:

Multi-source inputs → AI processing core → Intelligent output to user

This should feel like:

AI system architecture

Clean SaaS landing page

Modern, minimal, premium

✨ Motion (If Adding Animation)

Subtle flowing pulse animation along input lines

Slight floating motion on nodes

Gentle scale-in on load

🚫 Remove From Current Design

Remove horizontal progress bar

Remove step boxes

Remove labels below icons in boxed format

Remove orange underline connector

Remove card-like rectangular layout

🏁 Final Output Should Feel Like:

AI workflow diagram

Clean tech architecture visualization

Modern startup landing page section

Similar structure to the provided reference image