Perfect. I’m going to treat this like a real motion + product system spec for **SchemeSaathi** — clean, developer-friendly, and consistent.

---

# 🎛 Global Motion System (Design Tokens)

**Base Duration Scale**

* xs → 120ms
* sm → 180ms
* md → 240ms
* lg → 320ms
* xl → 480ms
* hero → 700–900ms

**Easing Tokens**

* `easeStandard` → cubic-bezier(0.4, 0.0, 0.2, 1)
* `easeOut` → cubic-bezier(0.0, 0.0, 0.2, 1)
* `easeIn` → cubic-bezier(0.4, 0.0, 1, 1)
* `easeSpring` → spring(1, 90, 12, 0)

**Scroll Trigger Offset**

* trigger at `viewport 80%`
* stagger default: `+80ms`

---

# 🟠 Hero Section

```
Hero Section

section-bg: 1200ms easeOut [α0→1, bg-position 0%→100%] onLoad subtle gradient drift ∞ alternate

headline: 800ms easeOut [α0→1, Y+40→0] onLoad stagger+120ms
highlight-word: 2000ms ∞ [bg-position 0%→200%] gradient shimmer loop
subtext: 600ms easeOut [α0→1, Y+20→0] delay+200ms

primary-cta (DottedButton): 240ms easeStandard [S1→1.03] onHover
primary-cta: 120ms easeIn [S1→0.97] onPress
primary-cta shadow: 240ms easeOut [shadow 0→4px offset] onHover

secondary-cta: 240ms easeStandard [bg neutral→accent, α0.9→1] onHover

hero-video-trigger: 240ms easeOut [S1→1.05, rotate 0→2°→0°] onHover subtle bounce
```

---

# 🧠 “How It Works” Process Section

```
Process Timeline

section-enter: 700ms easeOut [α0→1] onScroll

step-card (group): 600ms easeOut [α0→1, Y+30→0] stagger+100ms onScroll

step-icon: 2000ms ∞ [S1→1.05→1] subtle breathing
step-icon-hover: 240ms easeStandard [rotate 0→6°→0°] onHover

progress-line: 800ms easeOut [width 0→100%] onScroll
active-step: 240ms easeStandard [bg neutral→accent-light]
```

---

# 🟣 Feature Cards (Voice-first, Zero-form, etc.)

```
Feature Cards Grid

card-enter: 600ms easeOut [α0→1, Y+40→0] stagger+100ms onScroll

card-hover:
  240ms easeStandard [Y0→-6px, S1→1.02]
  240ms easeOut [shadow subtle→lg]

card-leave: 200ms easeIn [Y-6→0, S1.02→1]

icon-loop: 3000ms ∞ [rotate -3°→3°→-3°] subtle idle motion

card-border-glow: 1500ms ∞ [α0.2→0.5→0.2] soft pulse
```

---

# 🏢 Logo / Tech Stack Showcase

```
Logo Grid

logo-enter: 500ms easeOut [α0→1, Y+20→0] stagger+60ms

logo-idle: 3000ms ∞ [α0.6→1→0.6] subtle pulse
logo-hover: 200ms easeStandard [S1→1.1, grayscale 100%→0%]

marquee-scroll (optional): linear 20s ∞ [X0→-100%]
```

---

# 🔘 Buttons (DottedButton + shadcn Button)

```
Buttons

default-hover:
  240ms easeStandard [translate -4px, shadow 0→4px offset]
  border-radius 16px→8px

active-press:
  120ms easeIn [translate 0, shadow none]

focus-ring:
  180ms easeOut [outline α0→1, ring-scale 0.95→1]

loading-state:
  spinner 800ms linear ∞ rotate
  label α1→0.5
```

---

# 🧭 Navbar + Scroll Behavior

```
Navbar

navbar-enter: 600ms easeOut [Y-100→0, α0→1] onLoad

navbar-scroll:
  300ms easeStandard [bg transparent→solid, blur 0→12px]
  shadow 0→sm

nav-link-hover:
  200ms easeOut [underline scaleX 0→1]

mobile-menu-open:
  320ms easeOut [X100%→0]
mobile-menu-overlay:
  240ms easeOut [α0→0.6]
```

---

# 📂 Flyout Dropdown (FlyoutLink.jsx)

```
Flyout Menu

trigger-underline:
  300ms easeOut [scaleX 0→1] onHover

flyout-enter:
  300ms easeOut [α0→1, Y+15→0]

flyout-exit:
  200ms easeIn [α1→0, Y0→15]

menu-item-hover:
  180ms easeStandard [bg neutral→accent-light]
  180ms easeOut [X0→4px]

arrow-tip:
  240ms easeOut [rotate 45°→45° + S1→1.05]
```

---

# 🪟 Login Card

```
Login Card

card-enter:
  500ms easeOut [α0→1, Y+30→0]

input-focus:
  180ms easeOut [border neutral→accent]
  180ms easeOut [shadow 0→glow]

input-error:
  120ms easeIn [X0→-4→4→0] shake
  border accent→red

success-state:
  300ms easeOut [border→green, checkmark scale 0→1]

google-btn-hover:
  200ms easeStandard [bg neutral→slightly darker]
```

---

# 🧾 Forms (Input Fields)

```
Input Fields

label-float:
  200ms easeOut [Y0→-12px, S1→0.85]

placeholder:
  180ms easeOut [α0.6→0]

validation-message:
  200ms easeOut [α0→1, Y+5→0]

submit-loading:
  button disabled α1→0.6
  spinner rotate 800ms linear ∞
```

---

# 🪄 Tooltips / HoverCard

```
Tooltip / HoverCard

tooltip-enter:
  180ms easeOut [α0→1, Y+8→0]

tooltip-exit:
  120ms easeIn [α1→0, Y0→8]

hover-trigger:
  180ms easeStandard [S1→1.03]
```

---

# 📊 Dashboard / Sidebar

```
Sidebar

sidebar-expand:
  300ms easeOut [width 64px→240px]

menu-item-hover:
  180ms easeStandard [bg neutral→accent-light]
  icon S1→1.1

active-item:
  240ms easeOut [bg accent-light, left-indicator scaleY 0→1]

card-stats-enter:
  500ms easeOut [α0→1, Y+20→0] stagger+80ms

chart-line-draw:
  800ms easeOut [stroke-dashoffset 100%→0]
```

---

# 🧱 Image Card

```
Image Card

image-hover:
  300ms easeOut [S1→1.05]
  300ms easeOut [overlay α0→0.4]

caption-enter:
  240ms easeOut [Y+10→0, α0→1]
```

---

# 📜 Scroll Area

```
Scroll Area

scrollbar-hover:
  180ms easeStandard [width 4px→8px]

content-enter:
  400ms easeOut [α0→1]

scroll-shadow:
  240ms easeOut [α0→0.5] onScrollStart
```

---

# 📱 Mobile Interactions

```
Mobile

tap-feedback:
  120ms easeIn [S1→0.96]

bottom-sheet-open:
  320ms easeOut [Y100%→0]
  overlay α0→0.6

swipe-close:
  drag Y0→100%
  release threshold 40%

mobile-nav-item:
  200ms easeOut [X-20→0, α0→1] stagger+50ms
```

---

# 🎤 Icon Microinteractions (SchemeSaathi Personality)

```
Voice Icon:
  pulse 2000ms ∞ [S1→1.08→1]
  listening-state ripple 1200ms ∞ [ring S1→1.4, α0.4→0]

WhatsApp Icon:
  hover 240ms easeOut [rotate -6°→6°→0°]

AI Brain Icon:
  glow 2500ms ∞ [α0.4→0.8→0.4]
```

---

# 🧲 Scroll-Based Transitions

```
Scroll Animations

fade-in:
  600ms easeOut [α0→1, Y+30→0]

parallax-bg:
  linear [Y0→-80px] scroll-linked

section-pin (optional storytelling):
  sticky until next section intersects
  progress bar scaleX 0→1
```

---

# 🔥 CTA Section (Access What You Deserve)

```
CTA Section

section-enter:
  700ms easeOut [α0→1, Y+40→0]

cta-button:
  240ms easeStandard [S1→1.05]
  glow 1500ms ∞ [shadow subtle→soft accent pulse]

background:
  4000ms ∞ alternate [gradient shift 0%→100%]
```

---

# =========================

# USER FLOW — SchemeSaathi Landing → WhatsApp Conversion

# =========================

```
## User Flow

A[User Lands on Homepage] 
→ B{First Visit?}

B →|Yes| C[Play Hero Entrance Animations]
B →|No| D[Skip to Static Loaded State]

C → E[User Scrolls]
D → E[User Scrolls]

E → F{Clicks "Chat on WhatsApp"?}

F →|Yes| G[System Opens WhatsApp API Link +14155238886]
G → H[Prefilled Intro Message Sent]
H → I[User Starts Conversation]

F →|No| J[User Continues Scrolling]

J → K{User Watches How It Works?}
K →|Yes| L[Open Video Modal]
L → M[Play Video]
M → N{Closes Modal?}
N →|Yes| J

J → O{Clicks Feature Card?}
O →|Yes| P[Scroll to Explanation Section]
P → Q[Highlight Relevant Feature]

J → R{Clicks Navbar Pricing/Directory?}
R →|Yes| S[Open Flyout Menu]
S → T{Select Option?}
T →|Yes| U[Navigate to Page]

U → V{Clicks CTA?}
V →|Yes| G
```

---

If you want next-level polish, I can now:

* Redefine your **visual aesthetic system** (spacing, shadows, depth layers, glass vs soft UI)
* Or convert this into a **Framer Motion config-ready object**
* Or design the **dashboard motion system for the WhatsApp admin panel**

Tell me which layer you want to elevate next.
